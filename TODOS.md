# TODOS — 已决策延后的事项

每条都必须写清**触发条件**：什么情况下应该把它捡回来。没有触发条件的条目就是永久垃圾。

---

## 1. 自研流式 fMP4 → 渐进式 MP4 转换器

**现状**：`--container mp4` 走 `@invintusmedia/tomp4` 的 `convertFmp4ToMp4()` + box 级合并，内存是输入文件大小的约 4.7 倍（实测 337MB 输入 → RSS 1986MB），已有预检护栏，超阈值直接报错并给出去路。

**触发条件**：有用户撞上内存护栏，且其场景**确实必须**要渐进式输出（不能改用系统 ffmpeg、不能接受 `--no-mp4` 保留分离流）。

**为什么现在不做**：需要从 `moof`/`trun` 重建 `stts` / `ctts` / `stss` / `stsc` / `stsz` / `stco`。其中 `ctts`（B 帧显示顺序）与 `stss`（关键帧表）写错会产出"能播但 seek 花屏 / 时间戳错乱 / 音画不同步"这类最难定位的故障。为一个可选逃生通道承担这个风险不划算。

**届时的大致做法**：两遍扫描。第一遍只读 `moof` 头，统计采样数并算出 `moov` 大小；第二遍流式拷 `mdat` 数据并回填 `moov`。内存恒定。工作量约为现有 `mp4.ts` 的两倍。

---

## 2. 完整错误码体系

**现状**：`DownloadFailure` 只有 `code` 字段（`E_HTTP` / `E_MERGE` / `E_EXPIRED_URL` / `E_DISK` / `E_UNSUPPORTED`），其余错误仍是自然语言。

**触发条件**：Agent 调用方反馈需要按错误类型分支处理，或错误信息被用于自动化重试决策。

**为什么现在不做**：错误分类是一个需要独立设计的议题（跨命令、跨层次），塞进当前批次会变成半成品。

---

## 3. eslint / prettier

**现状**：无 lint 配置，只有 `tsc --noEmit`。CI 只跑 typecheck / test / build。

**触发条件**：多人协作，或出现反复的风格类 review 往返。

---

## 4. 未实测的编码矩阵

**现状**：容器搬运与 codec 无关（不解析 NAL），AVC / HEVC / AV1 已实测通过。以下无真实样本：

- E-AC-3（音频 id 30250）
- Hi-Res FLAC（音频 id 30251）
- 杜比视界（视频）

**触发条件**：拿到上述编码的真实可下载样本。

---

## 5. 分片失败的自动跨会话后台重试

**现状**：失败保留 `<dest>.part` + `.partstate.json`，用户重跑同一条命令即续传。

**触发条件**：出现"Agent 无人值守长任务"的实际需求（当前重跑一条命令即可，自动化收益不足）。
